// @file: device-llm/types/response.ts
// 类型定义已移至 @itookit/common/interfaces/llm，此文件保留向后兼容的重新导出。
export type {
    ToolChoice,
    ResponseFormat,
    ChatCompletionParams,
    ChatCompletionResponse,
    AssistantMessage,
    FinishReason,
    TokenUsage,
    Citation,
    ChatCompletionChunk,
} from '@itookit/common';
