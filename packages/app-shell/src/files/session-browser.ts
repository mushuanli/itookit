/** Compatibility re-export. Implementation lives in @itookit/app-core. */
export { createSessionBrowser, resolveBrowserTarget, taskSummary } from '@itookit/app-core';
export type { BrowserTarget, SessionBrowserDependencies } from '@itookit/app-core';
