/** Compatibility re-export. Implementation lives in @itookit/app-core. */
export { createSessionBrowser, resolveBrowserTarget, taskSummary, taskKeyEvent,
    exportSessionBundle, importSessionBundle, isSessionBundle, SessionLifecycleService,
    SESSION_BUNDLE_FORMAT, SESSION_BUNDLE_VERSION } from '@itookit/app-core';
export type { BrowserTarget, SessionBrowserDependencies, SessionExport } from '@itookit/app-core';
