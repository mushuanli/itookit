{
  "name": "@mdxeditor/renderer",
  "version": "1.0.0",
  "description": "Extensible Markdown renderer with plugin system",
  "main": "dist/index.js",
  "module": "dist/index.mjs",
  "types": "dist/index.d.ts",
  "files": [
    "dist",
    "styles"
  ],
  "scripts": {
    "build": "tsup",
    "dev": "tsup --watch",
    "type-check": "tsc --noEmit"
  },
  "keywords": [
    "markdown",
    "renderer",
    "plugin",
    "mathjax",
    "latex"
  ],
  "dependencies": {
    "@itookit/common": "workspace:*",
    "@itookit/vfs-core": "workspace:*",
    "marked": "^11.0.0"
  },
  "devDependencies": {
    "@types/node": "^20.10.0",
    "esbuild": "^0.27.0",
    "tsup": "^8.0.0",
    "typescript": "^5.3.0"
  },
  "peerDependencies": {
    "@itookit/vfs-core": "^1.0.0"
  },
  "peerDependenciesMeta": {
    "@itookit/vfs-core": {
      "optional": true
    }
  }
}
