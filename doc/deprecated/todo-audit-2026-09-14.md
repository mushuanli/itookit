# TODO 全任务审计快照（2026-09-14）

审计对象：[doc/todo.md](../todo.md)。基线 HEAD 为 `561a5d62`，读取时工作树已有四处未提交修改：TODO、验收记录、事件流文档及聊天节点 CSS。本报告以读取到的工作树内容为准，不把 HEAD 当作这些文件的完整版本。原始条目及 SHA-256 保存于同目录 `todo-audit-2026-09-14.json`。这是一次审计快照，不是新的活任务清单。

## 审计结论与范围

完整解析了 22 个编号任务、18 条 D 系列能力说明，以及一个未编号的样式工作包。编号任务中 9 项勾选、13 项开放；加上样式工作包，共 14 个开放工作包。P0 为 3/6 勾选，P1 为 4/10，P2 为 2/6。40.9% 仅是编号任务勾选比例，不能表示工作量或设计完成度。剩余工作拆成 61 个子项；107 个样式豁免中，58 个明确待补、1 个待复核，另 48 个属于选择器或状态修饰组。

审计覆盖全部 TODO 条目、所引用验收记录的关键章节、Durable 证据映射、Skill/Flow/VFS 设计以及针对性代码和测试入口核对。没有重新运行全仓测试、GUI、云模型或跨主机实验；“已有证据”指仓库记载和可定位入口，不等于本轮重新验收。已完成项暂保留原勾选，未发现足以要求全部撤销勾选的依据。

## 发现（按影响排序）

| 编号 | 影响 | 证据与判断 | 建议修正 |
| --- | --- | --- | --- |
| A01 | 高：重复开发与错误排期 | TODO:59 写发送边界尚未接线，:60 又记录完成接线；:65、:67 仍列关闭入口缺失，:63 已记真实窗口关闭保留记录。`apps/tauri-app/src/main.ts:294` 已接 Provider 回调，`packages/app-shell/src/core/SessionWorkbench.ts:75` 已有关闭入口，`packages/app-core/src/session/session-lifecycle.ts:31` 实现保留记录的关闭。 | P0-02 只保留最新状态；将旧判断移入历史，不重做已完成入口。超时剩余收窄为未覆盖的文本/三态/故障边界，普通模型超时已有持久与 live 收敛记录。 |
| A02 | 高：设计与任务清单互相否定 | `design/durable-harness-evidence.md:138,148` 仍写缺 Web/Tauri worktree 装配，TODO P1-03 已记录 Tauri IPC/bwrap 探针；映射 :147 称 P1-04 完成，TODO 保留真实窗口验收；`design/flow-execution-model.md:832` 又把图级下游重算列为未完成。`durable-flow-executor.test.ts:1119` 已有下游重算回归。 | 同步为“实现已有、指定验收未完成”；Web 无宿主通道的拒绝行为单列为平台边界，不能笼统列为漏装配。 |
| A03 | 高：性能结论强于证据 | 发送样本为 2870 ms/1301 IPC 与 2731 ms/1173 IPC，确实均未达标；两点不足以证明线性关系或唯一瓶颈。动作终点为响应头，外部测量终点为 mock 收齐请求，样本一相差 226 ms，不能称为相同边界。 | 统一指标定义；区分发送预处理、请求传输、Provider 等待，统计调用耗时及关键路径。把“IPC 是瓶颈”标为待验证的主要假设；原子批量优化后以同配置重复真实窗口测量。 |
| A04 | 高：共享工作重复且责任模糊 | 停止三态横跨 P0-02/P1-05/P1-10；跨主机租约横跨 P1-02/P1-03/P1-05；挂载与旧句柄 GUI 横跨 P0-04/P2-04；Skill 入口与生命周期横跨 P0-00/P2-01。 | 每个公共验收设主任务，其余引用同一证据。不能将同一场景重复计数，也不能因此删掉各任务独有要求。 |
| A05 | 中：完成范围不清 | P0-00 已勾选，但 §4 仍优先补 P0-00；P2-05 明确只完成“已记录范围”，不能承担全设计语义审计完成结论。 | P0-00 保留完成，深 cwd Skill 级联归 P0-04/P2-01；另列本轮发现的文档修正任务，不能借 P2-05 勾选将其隐藏。 |
| A06 | 中：实现限制不能直接证明原需求失效 | P0-04 将“嵌套挂载”收窄为单挂载内目录树，引用依据是当前 at 只支持单段；同时明确深 cwd Skill 级联未覆盖。 | 当前无法表达嵌套挂载可作为支持边界；若旧有效设计要求嵌套挂载，需要明确取代决策，不能仅凭当前代码删除要求。深 cwd 场景保留独立验收。 |
| A07 | 中：可复现最终矩阵不完整 | TODO 称“11 个阶段”，验收记录实际表列 12 行，额外包含调度器测试；TODO 手工命令块没有该项，也未显式构建 Web/其他应用。`pnpm test` 会运行调度器，独立包测试命令则不会。1667 = 1508 + 108 + 12 + 36 + 3，算术正确。 | 统一使用测试矩阵入口或列全各阶段；补明确宿主构建范围。最终验收绑定最终树和未提交差异，不能沿用 `3c3afe5d` 的通过结论。 |
| A08 | 中：样式工作包未编号 | 样式清单没有优先级/任务 ID；allowlist 同时含无需规则的选择器与明确待补样式。 | 为工作包分配稳定 ID；只处理标记待补和待复核类名，逐组验收后移除豁免，不能把全部 107 项视作缺 CSS。 |
| A09 | 中：Skill 设计剩余列表陈旧且有漏映射风险 | `design/skill-design.md:139` 下仍列初始化工具、独立 skill.load、CLI 文件来源为待完成，而同文件后文及 TODO 已记录实现；“运行中项目文件 watch”在 TODO 仅以跨进程通知限制出现。 | 清除陈旧待办；明确文件 watch 是仍有效需求还是已决策边界。若仍有效，显式纳入 P2-01，不能以通知仅进程内一句话替代任务。 |
| A10 | 中：已知环境限制被混在实现缺口中 | OCI、其他 OS、GTK Portal、发布打包、真实供应商均需要不同环境；本轮没有重测其可用性。 | 分开记录所需环境、缺实现、缺验收和产品支持决定。Linux mock/Xvfb 通过不得扩张为云端或其他平台通过。 |
| A11 | 中：维护规则未落实 | TODO §5 要求每项只留当前状态，但 P0-02 已累积多轮日志并产生矛盾；映射仍含旧测试数量和旧缺口。 | 将过程移入验收记录；每个任务只保留已实现、剩余、证据三段。测试数量始终附版本，不累计不同快照。 |
| A12 | 待核查：安装配置一致性 | 本轮 pnpm 10.20.0 执行检查时报告 `pnpm.overrides` 被忽略；根 package.json 有 overrides，pnpm-workspace.yaml 未见该配置。 | 纳入 P0-05 全新安装验证，核实覆盖规则是否生效及锁文件结果。本轮仅记录实际告警，不据此断言依赖已错误解析。 |
| A13 | 高：工作树删去了比现有 TODO 更新的证据 | 用户追加提交要求后检查完整 Git 差异，发现验收记录约 105 行删除，TODO 也删除发送失败、外部租约拒写、pause 投影及 `561a5d62` 的 1670 项阶段回归记录。这些内容仍在 HEAD；`SessionWorkbench.ts:404` 确有 pause-pending/paused，CLI crash-matrix :269 已检查 nodeTaskIds 自洽性。 | 本次不提交这些证据删除。工作树快照中的任务统计不变，但恢复被删证据后，P0-02/P0-05 的已实现和最新阶段记录需要补回；不能仅据工作树缺文档推断功能不存在。 |

## 全部任务的归一化状态

“保留完成”仅适用于原条目写明的范围；“部分完成”需要继续实施或验收；“协议未实现”不能由既有相邻能力替代。

| ID | 标题 | 审计状态 | 现有能力与证据判断 |
| --- | --- | --- | --- |
| P0-00 | 两端核心装配一致性 | 保留完成 | 共用装配、项目规则、持久 Skill 身份及面板操作已有分层记录；深 cwd 级联不是此次完成范围。 |
| P0-01 | 完整 Tauri 调用链 | 保留完成 | 窗口到 Bash/CLI DAG 及重开链路已记录；Provider 为本地 mock，失败场景仍归 P0-02。 |
| P0-02 | 应用级失败、取消与性能闭环 | 部分完成，P0 阻塞 | 关闭保留入口和计量代码存在；性能未达标，设备未确认停止及其他失败窗口仍开放。 |
| P0-03 | 可复用运行入口与交付说明 | 保留完成 | 操作说明与已决策的明文凭证边界已记录；不表示安装包已经交付。 |
| P0-04 | 平台实机验证 | 部分完成，环境与范围待闭合 | Linux/Xvfb/bwrap 有证据；GTK 完整选择、修复后窗口行为、发布及其他平台未验收。 |
| P0-05 | 当前最终回归 | 待最终基线 | 历史批次已有通过记录，当前 HEAD 与工作树已不同；其余有效要求闭合后再最终验收。 |
| P1-01 | 任意崩溃点的持久调度 | 部分完成，故障矩阵缺口 | 稳定请求身份、恢复和 indeterminate 策略已有；不能由有限 SIGKILL 用例推导任意崩溃点正确。 |
| P1-02 | 通用调度所有权与 fencing | 部分完成，协议与跨主机验收缺口 | 本机租约、epoch、skew 预算已有；共享存储原子性和真实时钟保证尚未闭合。 |
| P1-03 | 工作区与后台委派恢复 | 部分完成，平台与恢复验收缺口 | CLI/Tauri worktree 装配和部分 SIGKILL 窗口已有；OCI、交互和遗留意图边界仍缺。 |
| P1-04 | 图级 retry 与运行控制 | 实现已有，缺窗口验收 | 下游重算、委派组和 token 退款已有回归；合成委派子节点直接作重试源仍拒绝。 |
| P1-05 | Durable 五篇设计完整映射 | 部分完成，多个实现与证据缺口 | authority CAS、receipt 确认、cache 清理等已有；执行端 fence、记录族与完整故障矩阵仍缺。 |
| P1-06 | Transcript 存储分页与导出 | 保留完成 | 物理分页、固定版本、字节裁剪与导出已有入口；10k Effect 守卫及裁剪边界保留。 |
| P1-07 | Schema/输出策略 | 部分完成，契约未闭合 | schema 子集兼容、无人消费输出验证已有；端口 repair/continue 与节点输出编译仍缺。 |
| P1-08 | Effect 回滚缺陷 | 保留完成 | 回滚仅限新加载及错误聚合已有回归入口，不能扩张为 Skill 全生命周期完成。 |
| P1-09 | CLI supervisor 累积结果 | 保留完成 | Flow 测试可定位跨 supervisor 轮次累积用例；普通 Loop 保持独立语义。 |
| P1-10 | CLI 非交互监控窗口 | 保留完成 | live 句柄及时发布已有回归入口；桌面监控和取消不计入此完成项。 |
| P2-01 | Skill 完整生命周期 | 部分完成，版本与委派协议缺口 | 快照、交集、glob 和 slash 已有；严格恢复版本、自动委派和系统性竞态未闭合。 |
| P2-02 | 跨 Session Memory | 共享协议未实现 | 当前隔离行为已验证；同 namespace 和 Session shared state 均不构成跨 Session 共享。 |
| P2-03 | Memory 模型写入与管理 | 部分完成，实现与验收混合 | CAS、工具、UI 和部分崩溃恢复已有；GUI/云模型/GC、语义检索和压缩仍缺。 |
| P2-04 | VFS/C4 完整验收 | 部分完成，矩阵未闭合 | 权限和旧句柄回归已有；真实可观察旧句柄路径及多层功能故障矩阵未闭合。 |
| P2-05 | 文档同步审计（已记录范围） | 保留阶段完成，新增修正开放 | 历史审计不撤销；A01–A11 说明当前文档仍需纠正，不能称全量语义同步完成。 |
| P2-06 | app-core 包内边界与技术债 | 保留完成 | 原条目列举的整理批次有验证记录；新增功能和全仓回归仍受 P0-05 管理。 |
| STYLE | 样式待办（审计临时 ID） | 开放，原文未定优先级 | 明确待补组和待复核组需单独处理；CDN 使用是记录的现状，并未授权本轮替换。 |

## 剩余工作拆解与验收出口

以下子项编号仅用于本审计，可用于后续排期；不是新增需求。部分原文大项仍需依据有效设计枚举具体故障窗口，不能将这些子项数量视为穷尽的测试用例数。建议出口用于使任务可验收，新增阈值或支持范围仍需明确决策。

| 父任务 | 子项 | 工作与验收出口 |
| --- | --- | --- |
| P0-02 | 01 | 定义统一发送计量边界与通道覆盖；日志可辨正常响应、失败和超时结束，不混用 Node 逻辑调用与桌面 IPC。 |
| P0-02 | 02 | 原子批量降低往返；保持事务内跨进程恢复检查，同配置真实窗口达到 ≤2 秒且 ≤100 次调用，并复核空闲目标。 |
| P0-02 | 03 | 注入真实窗口 IPC 故障与发送回滚；界面、持久记录及重开结果一致且可重试。 |
| P0-02 | 04 | 注入真实窗口保存失败/并发保存及重试；dirty 和输入保留，重试后磁盘与界面一致。 |
| P0-02 | 05 | 设备不确认停止时执行关闭/删除；有界失败并保留数据，不能显示已物理停止。 |
| P0-02 | 06 | 真实 GUI 核验 cancel/pause 的请求接受、逻辑变化、外部停止三态及重开一致性；复用 P1-05 证据。 |
| P0-02 | 07 | 在最新 UI 上补超时/取消文本与监控收敛断言；已有超时连接关闭、持久原因和 0.75–1.53 秒 live 收敛记录不重记为新实现。 |
| P0-04 | 01 | 明确支持 OS 和功能矩阵，对声明支持的平台取得真实行为证据。 |
| P0-04 | 02 | 在可用 GTK/Portal 环境完成选择→授权→挂载；分别验证取消、reject、可能的挂起及修复后的错误反馈。 |
| P0-04 | 03 | 构建并验证发布安装包；记录依赖、字体和运行边界，处理过期受版本控制产物。 |
| P0-04 | 04 | 区分单挂载目录树与嵌套挂载需求；对深 cwd 下 Skill 级联增加真实宿主验收，并保留内层 AGENT 不覆盖规则。 |
| P0-04 | 05 | 核对列表图标在声明支持环境中的可读性和字体依赖；字体面加载成功不替代图标呈现验收。 |
| P0-05 | 01 | 冻结最终版本及未提交差异；记录 Node/pnpm/Rust、平台、命令和依赖来源。 |
| P0-05 | 02 | 全新依赖安装与冻结锁文件核验；调查本轮 overrides 告警，不能只复用缓存。 |
| P0-05 | 03 | 最终类型/文档/样式、库及目标应用/原生构建；明确 Web 与发布安装包各自入口。 |
| P0-05 | 04 | 完整测试矩阵含调度器、包、CLI、独立 SIGKILL、Rust；记录每项跳过原因。 |
| P0-05 | 05 | 最终树真实窗口验收，复核所有已闭合要求；恢复普通构建并留存可复现证据。 |
| P1-01 | 01 | 从协议枚举提交前后与 checkpoint 间隙，真实进程故障后核对 Task 身份、fan-out 和迭代不重复。 |
| P1-01 | 02 | 为 delegation 提供可执行验收入口（当前 CLI schema 不暴露该配置）；验证委派真实进程故障恢复。 |
| P1-01 | 03 | 对已执行未提交的外部副作用验证 reconcile/indeterminate/显式重放；按故障点核对实际执行次数。 |
| P1-02 | 01 | 明确支持共享后端的 CAS/事务原子性、authority 时间与 skew 假设。 |
| P1-02 | 02 | 真实多进程/多主机竞争、租约到期和迁移；活拥有者不被抢占，旧 epoch 提交拒绝。 |
| P1-02 | 03 | 覆盖时钟偏差、网络分区、心跳丢失与延迟旧返回；记录收敛条件和不支持边界。 |
| P1-03 | 01 | 真实 OCI read-only 与 worktree+OCI；文件、进程 cwd、授权和拒写一致。 |
| P1-03 | 02 | 合并/冲突交互；验证 manual、discard、auto-if-clean 的用户可见结果及脏副本保留规则。 |
| P1-03 | 03 | 用户窗口启动工作区 Flow，真实桌面重启恢复同一工作区和 pending 收尾。 |
| P1-03 | 04 | 授权变化或 Kernel 未发现 Session 的遗留创建意图回收；不能删除仍有合法拥有者的副本。 |
| P1-03 | 05 | 复用 P1-02 的跨主机租约结论验证工作区接管，不以本机 SIGKILL 代替。 |
| P1-04 | 01 | 真实窗口图级重算、结果收敛提示及重开记录；验证下游新代、委派组、退款与拒绝边界。 |
| P1-05 | 01 | 将五篇设计逐条映射到实现、记录、故障证据；清除陈旧缺口并列明未覆盖行。 |
| P1-05 | 02 | 定义并验证 cache provider 能力差异，以及跨 Session retention/GC 竞争。 |
| P1-05 | 03 | 真实供应商重复计费核对；使用供应商可获得的请求/账单证据，区分 Kernel usageId 幂等。 |
| P1-05 | 04 | 资源 adapter 执行令牌 fencing；旧 epoch 即使延迟到执行端也不能继续产生未授权副作用。 |
| P1-05 | 05 | 多进程/多 store 迁移屏障和 account/allocation/export/import 记录族；迁移期间权威归属可核验。 |
| P1-05 | 06 | Storage §5 字段与记录族目标逐条闭合；已有 manifest 与部分 compaction 不重复实现。 |
| P1-05 | 07 | 补 stream/消息/receipt/compaction 完整故障矩阵，包含旧在途投递、混合版本和 replay 水位假设。 |
| P1-05 | 08 | 停止三态复用 P0-02；明确 SIGKILL 与存储事务内部/断电持久性证据的区别。 |
| P1-07 | 01 | 定义端口错误 repair/continue，保留原响应与校验错误；区分 Agent 内部已有策略。 |
| P1-07 | 02 | 明确节点输出契约，将 Agent responseFormat 编译为可引用端口 schema；不直接混用插件清单。 |
| P1-07 | 03 | 明确同名 plugin@version 宿主代码漂移策略，恢复行为与冻结 Run 定义一致。 |
| P2-01 | 01 | 实现 keep-old/require-reload/drift marker 版本协议，验证编辑、重开与恢复中的身份和正文一致性。 |
| P2-01 | 02 | Skill 自动委派编译到统一 TaskGroup；与 Flow 的身份、工具交集和恢复语义一致。 |
| P2-01 | 03 | 系统核验作用域销毁/重建、在途 I/O、排队操作与迟到通知，关闭后不能复活旧作用域。 |
| P2-01 | 04 | 判定运行中项目文件 watch 的有效需求并映射到任务；当前跨进程文件变更不触发通知。 |
| P2-02 | 01 | 设计独立生命周期的跨 Session 存储和共享身份；保留默认隔离。 |
| P2-02 | 02 | 实现显式读写授权、来源身份和审计，并验证跨 Session 越权拒绝。 |
| P2-02 | 03 | 定义并发一致性与生命周期/删除行为；验证新 CLI Run 在显式授权后按协议读取。 |
| P2-03 | 01 | 真实 Tauri 窗口管理 Memory，重开核对编辑保留和删除不复活。 |
| P2-03 | 02 | 真实云模型调用 Memory 工具，核验白名单、策略与实际持久结果。 |
| P2-03 | 03 | 条件写冲突后的人工处置；保留输入并明确摘要比较无法识别同内容删除重建的边界。 |
| P2-03 | 04 | 独立 retention/GC 故障和存储事务内部持久性验证；不将已有服务边界 SIGKILL 当作内部覆盖。 |
| P2-03 | 05 | 语义/向量检索策略及可衡量质量验证；词项匹配不算完成。 |
| P2-03 | 06 | 内容压缩策略及恢复/来源保留验证；容量和时间裁剪不算内容压缩。 |
| P2-04 | 01 | 旧句柄撤销在真实窗口的可观察路径，验证在途操作与撤销后的新调用。 |
| P2-04 | 02 | 按有效 VFS/C4 设计枚举浏览、编辑、工具、附件、平台故障矩阵；复用已有权限和 GUI 证据。 |
| STYLE | 01 | Agent 快捷 Prompt 样式组，交互和删除按钮呈现验收后移除对应豁免。 |
| STYLE | 02 | DAG 空态、运行视图与设置分组，空/运行/终态均可读。 |
| STYLE | 03 | 同步设置组，状态、错误、进度、冲突和日志可辨。 |
| STYLE | 04 | VFS 列表、移动弹窗与 mention 预览，检查空态、展开和选择。 |
| STYLE | 05 | mdx 打印与零散元素，分别检查屏幕和打印结果。 |
| STYLE | 06 | 复核未分类 agent-form-edit-link；先确认运行时/行内样式或纯选择器用途，再决定补样式或保留说明。 |

## 建议依赖顺序

1. 先修正 A01/A02/A05/A09 的状态冲突，统一证据口径与主任务归属，避免重复实现。
2. 推进 P0-02 原子批量优化及失败 GUI，同时准备 P0-04 的平台/Portal/发布环境。
3. P1-02 先明确共享存储与时间协议，支撑 P1-03 跨主机工作区和 P1-05 迁移/fencing；P1-01 本机故障矩阵可独立推进。
4. P1-04 窗口重算可与性能优化独立验收；P1-07 节点契约与版本漂移、P2-01 版本/委派应保持协议一致。
5. P2-03 Session 本地 GUI/冲突/GC 验收不依赖 P2-02；跨 Session 共享必须在 P2-02 授权与生命周期协议之后。
6. 样式各组可单独闭合；P0-05 是所有仍有效要求的最终闸门，不是只等待 P0-02/P0-04 的桌面回归。可先跑阶段矩阵，但不能提前称整体完成。

以上是建议依赖，不是代码已强制的全部先后关系。P1-05 是证据汇总入口，不应成为所有实现相互等待的循环依赖。

## 本轮验证

- `pnpm docs:check`：通过，76 份活文档，5 条历史表述告警。不能验证本文指出的语义冲突。
- `pnpm styles:check`：通过，63 样式表、844 markup 文件、3941 已定义类、107 豁免。不能证明待补组完成。
- `pnpm --version`：10.20.0；观察到 overrides 告警，待 P0-05 核查。
- 已核对发送边界接线、关闭保留记录、目录选择器异常区分及 graph retry/supervisor 测试入口。
- 本轮不修改 TODO 状态，不实现上述剩余功能，不提交代码。既有工作树修改保留。

机器可读文件保留全部任务原文、来源行、原始勾选、归一化判断、剩余子项、D 系列能力及样式分组，便于后续导入排期；自动解析未把说明性子弹点一律当作未完成任务。

## 追加提交要求后的核对

用户随后授权提交已完成且核验正确的功能。提交范围为历史取消原因区中性配色、事件说明、验收记录中相关补充及本审计快照。已有取消行为回归用于核对渲染语义，普通 Tauri 前端构建用于验证 CSS 打包；本轮不新增 GUI 通过声明。

本轮追加验证：`cancelled-history.test.ts` 与 `terminal-node-reason.test.ts` 共 6 项通过；`pnpm --filter tauri-app build` 通过（有既有体积分块提示）。

A13 补充了 HEAD 与工作树的证据差异：pause 投影已经实现，窗口入口/真实 GUI 三态仍开放；Provider 连接失败可续发和外部租约拒写保留草稿已有 HEAD 中的历史窗口记录，但不覆盖所有 IPC/提交失败，也不证明短时错误 toast 可见。最新已提交阶段证据为 `561a5d62` 的 1670 项，而非工作树仅保留的 1667 项；两者都不包括本轮 CSS 修正的最终 GUI 验收。JSON 保留最初工作树指纹，使用者应同时读取该差异说明。
